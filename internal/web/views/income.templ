package views

import (
	"fmt"
	"strconv"
	"time"

	"github.com/sbengtson/budget/internal/core/money"
	"github.com/sbengtson/budget/internal/core/store"
	"github.com/templui/templui/components/button"
	"github.com/templui/templui/components/card"
	"github.com/templui/templui/components/dialog"
	"github.com/templui/templui/components/input"
	"github.com/templui/templui/components/table"
)

type IncomeData struct {
	Month     string // YYYY-MM
	PrevMonth string
	NextMonth string
	Today     string
	Rows      []store.Income
	Total     int64
	Actual    int64
	Budgeted  int64
}

templ IncomePage(d IncomeData) {
	@Layout("Income · Budget", "budget") {
		<div class="mb-4 flex items-center justify-between gap-4">
			<h1 class="text-2xl font-semibold tracking-tight">Income · { incomeMonthLabel(d.Month) }</h1>
			<div class="flex items-center gap-2">
				@button.Button(button.Props{Href: "/budget/income?month=" + d.PrevMonth, Variant: button.VariantOutline, Size: button.SizeSm}) {
					‹ prev
				}
				@button.Button(button.Props{Href: "/budget/income?month=" + d.Today, Variant: button.VariantOutline, Size: button.SizeSm}) {
					today
				}
				@button.Button(button.Props{Href: "/budget/income?month=" + d.NextMonth, Variant: button.VariantOutline, Size: button.SizeSm}) {
					next ›
				}
			</div>
		</div>

		<div class="mb-6 grid grid-cols-2 gap-3 md:grid-cols-4">
			@incomeStat("Estimated", money.Format(d.Total), "text-emerald-500")
			@incomeStat("Actual", money.Format(d.Actual), "text-emerald-500")
			@incomeStat("Budgeted", money.Format(d.Budgeted), "")
			@incomeStatSigned("Remain", d.Total-d.Budgeted)
		</div>

		<div class="mb-3 flex flex-wrap items-center justify-between gap-3">
			<p class="text-xs text-muted-foreground">
				<a class="underline underline-offset-4 hover:text-foreground" href={ templ.URL("/budget?month=" + d.Month) }>← back to budget</a>
			</p>
			<div class="flex flex-wrap items-center gap-2">
				@button.Button(button.Props{
					Variant: button.VariantOutline,
					Size:    button.SizeSm,
					Attributes: templ.Attributes{
						"hx-post":    "/budget/income/copy-prev?month=" + d.Month,
						"hx-confirm": "Copy all entries from the previous month into this one?",
						"title":      "Copy every entry from " + d.PrevMonth + " into this month",
					},
				}) {
					← copy from prev
				}
				@button.Button(button.Props{
					Href: "/budget/income/new?month=" + d.Month,
					Size: button.SizeSm,
					Attributes: templ.Attributes{
						"hx-get":    "/budget/income/new?month=" + d.Month,
						"hx-target": "#modal",
						"hx-swap":   "innerHTML",
					},
				}) {
					+ new
				}
			</div>
		</div>

		if len(d.Rows) == 0 {
			<p class="text-sm text-muted-foreground">
				No income entries yet for this month. Add one with the <strong>+ new</strong> button.
			</p>
		} else {
			@table.Table() {
				@table.Header() {
					@table.Row() {
						@table.Head() { Source }
						@table.Head(table.HeadProps{Class: "text-right"}) { Amount }
						@table.Head() { }
					}
				}
				<tbody id="income-rows" class="[&_tr:last-child]:border-0">
					for _, r := range d.Rows {
						@IncomeRow(d.Month, r)
					}
				</tbody>
			}
		}
	}
}

templ incomeStat(label, value, valueClass string) {
	@card.Card(card.Props{Class: "p-4"}) {
		<div class="text-xs uppercase tracking-wide text-muted-foreground">{ label }</div>
		<div class={ "mt-1 text-lg font-semibold tabular-nums " + valueClass }>{ value }</div>
	}
}

templ incomeStatSigned(label string, cents int64) {
	@card.Card(card.Props{Class: "p-4"}) {
		<div class="text-xs uppercase tracking-wide text-muted-foreground">{ label }</div>
		<div class="mt-1 text-lg font-semibold tabular-nums">
			@SignedMoney(cents)
		</div>
	}
}

templ IncomeRow(month string, r store.Income) {
	@table.Row(table.RowProps{Attributes: templ.Attributes{"id": fmt.Sprintf("income-%d", r.ID)}}) {
		@table.Cell(table.CellProps{Class: "font-medium"}) { { r.Name } }
		@table.Cell(table.CellProps{Class: "text-right tabular-nums text-emerald-500"}) {
			{ money.Format(r.AmountCents) }
		}
		@table.Cell(table.CellProps{Class: "whitespace-nowrap"}) {
			@button.Button(button.Props{
				Href:    fmt.Sprintf("/budget/income/%d/edit?month=%s", r.ID, month),
				Variant: button.VariantOutline,
				Size:    button.SizeSm,
				Class:   "mr-1",
				Attributes: templ.Attributes{
					"hx-get":    fmt.Sprintf("/budget/income/%d/edit?month=%s", r.ID, month),
					"hx-target": "#modal",
					"hx-swap":   "innerHTML",
				},
			}) {
				edit
			}
			@button.Button(button.Props{
				Variant: button.VariantDestructive,
				Size:    button.SizeSm,
				Attributes: templ.Attributes{
					"hx-delete":  fmt.Sprintf("/budget/income/%d?month=%s", r.ID, month),
					"hx-target":  fmt.Sprintf("#income-%d", r.ID),
					"hx-swap":    "outerHTML",
					"hx-confirm": "Delete?",
				},
			}) {
				×
			}
		}
	}
}

type IncomeFormData struct {
	Editing bool
	ID      int64
	Month   string
	Name    string
	Amount  string
}

templ IncomeForm(d IncomeFormData) {
	@dialogPanel("") {
		<form
			if d.Editing {
				hx-put={ fmt.Sprintf("/budget/income/%d?month=%s", d.ID, d.Month) }
			} else {
				hx-post={ "/budget/income?month=" + d.Month }
			}
			hx-target="#modal"
			hx-swap="innerHTML"
			class="grid gap-4"
		>
			@dialog.Header() {
				@dialog.Title() {
					if d.Editing {
						Edit income
					} else {
						New income
					}
				}
				@dialog.Description() {
					{ incomeMonthLabel(d.Month) }
				}
			}
			<div class="grid gap-3">
				@formRow("income-name", "Source") {
					@input.Input(input.Props{
						ID:          "income-name",
						Name:        "name",
						Value:       d.Name,
						Required:    true,
						Placeholder: "Salary",
						Attributes:  templ.Attributes{"autofocus": ""},
					})
				}
				@formRow("income-amount", "Amount") {
					@input.Input(input.Props{
						ID:          "income-amount",
						Name:        "amount",
						Value:       d.Amount,
						Placeholder: "0.00",
						Class:       "tabular-nums",
					})
				}
			</div>
			@dialog.Footer() {
				@button.Button(button.Props{
					Variant: button.VariantOutline,
					Type:    "button",
					Attributes:   templ.Attributes{"hx-on:click": "document.getElementById('modal').replaceChildren()"},
				}) {
					cancel
				}
				@button.Button(button.Props{Type: "submit"}) {
					save
				}
			}
		</form>
	}
}

func incomeMonthLabel(m string) string {
	t, err := time.Parse("2006-01", m)
	if err != nil {
		return m
	}
	return t.Format("January 2006")
}

// stringIDs is a helper that converts an int64 to a string for templ
// expressions; kept here so income.templ stays self-contained.
func stringIDs(v int64) string { return strconv.FormatInt(v, 10) }
