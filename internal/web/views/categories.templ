package views

import (
	"fmt"
	"strconv"

	"github.com/sbengtson/budget/internal/core/store"
	"github.com/templui/templui/components/badge"
	"github.com/templui/templui/components/button"
	"github.com/templui/templui/components/dialog"
	"github.com/templui/templui/components/input"
	"github.com/templui/templui/components/table"
)

type CategoriesData struct {
	Groups []store.CategoryGroup
	Cats   []store.Category
}

templ CategoriesPage(d CategoriesData) {
	@Layout("Categories", "categories") {
		<div class="mb-4 flex items-center justify-between gap-4">
			<h1 class="text-2xl font-semibold tracking-tight">Categories</h1>
		</div>

		<form
			hx-post="/categories/group"
			hx-target="body"
			hx-swap="outerHTML"
			class="mb-6 flex flex-wrap items-center gap-2"
		>
			<div class="w-64">
				@input.Input(input.Props{
					Name:        "name",
					Placeholder: "New group name",
					Required:    true,
					Class:       "w-full",
				})
			</div>
			@button.Button(button.Props{Type: "submit", Size: button.SizeSm}) {
				+ group
			}
		</form>

		for _, g := range d.Groups {
			<h2 class="mb-2 mt-6 text-sm font-semibold uppercase tracking-wide text-muted-foreground">
				{ g.Name }
			</h2>
			@table.Table() {
				@table.Header() {
					@table.Row() {
						@table.Head(table.HeadProps{Class: "w-[45%]"}) { Name }
						@table.Head(table.HeadProps{Class: "w-[20%] text-right"}) { Goal }
						@table.Head(table.HeadProps{Class: "w-[20%]"}) { Due }
						@table.Head(table.HeadProps{Class: "w-[15%]"}) { }
					}
				}
				@table.Body() {
					for _, c := range d.Cats {
						if c.GroupID == g.ID {
							@CategoryRow(c)
						}
					}
				}
			}
			<form
				hx-post="/categories"
				hx-target="body"
				hx-swap="outerHTML"
				class="mb-6 mt-2 flex flex-wrap items-center gap-2"
			>
				<input type="hidden" name="group_id" value={ strconv.FormatInt(g.ID, 10) }/>
				<div class="w-56">
					@input.Input(input.Props{
						Name:        "name",
						Placeholder: "new category name",
						Required:    true,
						Class:       "w-full",
					})
				</div>
				<div class="w-44">
					@input.Input(input.Props{
						Name:        "goal_cents",
						Placeholder: "goal $ (optional)",
						Class:       "w-full tabular-nums",
					})
				</div>
				<div class="w-44">
					@input.Input(input.Props{
						Type:  input.TypeDate,
						Name:  "goal_due",
						Class: "w-full",
					})
				</div>
				@button.Button(button.Props{Type: "submit", Size: button.SizeSm, Variant: button.VariantOutline}) {
					+ add
				}
			</form>
		}
	}
}

templ CategoryRow(c store.Category) {
	@table.Row(table.RowProps{Attributes: templ.Attributes{"id": fmt.Sprintf("cat-row-%d", c.ID)}}) {
		@table.Cell() {
			<span class="font-medium">{ c.Name }</span>
			if c.IsIncome {
				<span class="ml-2">
					@badge.Badge(badge.Props{Variant: badge.VariantOutline, Class: "text-muted-foreground"}) {
						🔒 system
					}
				</span>
			}
		}
		@table.Cell(table.CellProps{Class: "text-right tabular-nums text-muted-foreground"}) {
			if c.GoalCents != nil {
				${ formatCents(*c.GoalCents) }
			}
		}
		@table.Cell(table.CellProps{Class: "text-muted-foreground"}) {
			if c.GoalDueDate != nil {
				{ c.GoalDueDate.Format("2006-01-02") }
			}
		}
		@table.Cell(table.CellProps{Class: "whitespace-nowrap"}) {
			if !c.IsIncome {
				@button.Button(button.Props{
					Href:    fmt.Sprintf("/categories/%d/edit", c.ID),
					Variant: button.VariantOutline,
					Size:    button.SizeSm,
					Class:   "mr-1",
					Attributes: templ.Attributes{
						"hx-get":    fmt.Sprintf("/categories/%d/edit", c.ID),
						"hx-target": "#modal",
						"hx-swap":   "innerHTML",
					},
				}) {
					edit
				}
				@button.Button(button.Props{
					Variant: button.VariantDestructive,
					Size:    button.SizeSm,
					Attributes: templ.Attributes{
						"hx-post":    fmt.Sprintf("/categories/%d/archive", c.ID),
						"hx-confirm": "Archive this category?",
						"hx-target":  fmt.Sprintf("#cat-row-%d", c.ID),
						"hx-swap":    "outerHTML",
					},
				}) {
					archive
				}
			}
		}
	}
}

// CategoryFormData holds the editable surface of a category for the
// edit modal. Create uses the inline per-group form on the index page and
// does not go through this form.
type CategoryFormData struct {
	ID        int64
	GroupID   int64
	Name      string
	GoalCents string
	GoalDue   string
	Groups    []store.CategoryGroup
}

templ CategoryForm(d CategoryFormData) {
	@dialogPanel("") {
		<form
			hx-put={ fmt.Sprintf("/categories/%d", d.ID) }
			hx-target="body"
			hx-swap="outerHTML"
			class="grid gap-4"
		>
			@dialog.Header() {
				@dialog.Title() {
					Edit category
				}
			}
			<div class="grid gap-3">
				@formRow("cat-name", "Name") {
					@input.Input(input.Props{
						ID:         "cat-name",
						Name:       "name",
						Value:      d.Name,
						Required:   true,
						Attributes: templ.Attributes{"autofocus": ""},
					})
				}
				@formRow("cat-group", "Group") {
					@SelectField(SelectFieldProps{
						ID:   "cat-group",
						Name: "group_id",
					}, categoryGroupOptions(d.Groups, d.GroupID))
				}
				<div class="grid grid-cols-2 gap-3">
					@formRow("cat-goal", "Goal $") {
						@input.Input(input.Props{
							ID:          "cat-goal",
							Name:        "goal_cents",
							Value:       d.GoalCents,
							Placeholder: "(optional)",
							Class:       "tabular-nums",
						})
					}
					@formRow("cat-due", "Goal due") {
						@input.Input(input.Props{
							ID:    "cat-due",
							Type:  input.TypeDate,
							Name:  "goal_due",
							Value: d.GoalDue,
						})
					}
				</div>
			</div>
			@dialog.Footer() {
				@button.Button(button.Props{
					Variant: button.VariantOutline,
					Type:    "button",
					Attributes:   templ.Attributes{"hx-on:click": "document.getElementById('modal').replaceChildren()"},
				}) {
					cancel
				}
				@button.Button(button.Props{Type: "submit"}) {
					save
				}
			}
		</form>
	}
}

func categoryGroupOptions(groups []store.CategoryGroup, selected int64) []Opt {
	out := make([]Opt, 0, len(groups))
	for _, g := range groups {
		out = append(out, Opt{
			Value:    strconv.FormatInt(g.ID, 10),
			Label:    g.Name,
			Selected: g.ID == selected,
		})
	}
	return out
}

func formatCents(cents int64) string {
	dollars := cents / 100
	c := cents % 100
	if c < 0 {
		c = -c
	}
	return fmt.Sprintf("%d.%02d", dollars, c)
}
