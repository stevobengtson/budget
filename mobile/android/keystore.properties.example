# Copy to keystore.properties (untracked) and fill in real values.
# Paths are relative to mobile/android/ (rootProject).
storeFile=upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
