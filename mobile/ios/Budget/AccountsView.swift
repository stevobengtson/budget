import SwiftUI

// Placeholder — accounts list + drill-in to transactions arrive in a later step.
struct AccountsView: View {
    var body: some View {
        NavigationStack {
            ComingSoon(title: "Accounts", systemImage: "building.columns")
                .navigationTitle("Accounts")
        }
    }
}
